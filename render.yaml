services:
- type: web
  name: mediscan-ai
  env: python
  buildCommand: pip install pip --upgrade && pip install -r requirements.txt --only-binary=:all:
  startCommand: gunicorn app:app
  envVars:
  - key: PYTHON_VERSION
    value: 3.12.7

